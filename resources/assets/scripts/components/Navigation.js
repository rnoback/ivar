//import { equalOrLargerThanBreakpoint, smallerThanBreakpoint } from "../util/responsive"

//const enhancer = '[data-enhancer="Navigation"]';

export default class Navigation
{

	// elements
	// $eHtml = $('html');
	// $ebanner = $('banner');
	// iStickyPointY = 0;
	//
	// constructor(eNavigation) {
	// 	// sticky menu calculation
	// 	this.iStickyPointY = this.eMain.offsetTop;
	// 	this.navSticky();
	// 	$(window).on('scroll resize onorientationchange', () => { this.navSticky() });
	// }
	//
	// navSticky() {
	// 	let docPosY = $(window).scrollTop();
	// 	if (docPosY >= this.iStickyPointY) {
	// 		this.$eHtml.addClass('sticky-nav');
	// 	} else {
	// 		this.$eHtml.removeClass('sticky-nav');
	// 	}
	// }
}
